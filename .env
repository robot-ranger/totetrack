# Environment variables for local development
# Based on fastapi/full-stack-fastapi-template naming, mapped to this app

# Security
SECRET_KEY=devsecret-change-me
ACCESS_TOKEN_EXPIRE_MINUTES=60
PASSWORD_RESET_TOKEN_EXPIRE_MINUTES=30

# Initial superuser (created on app startup if not present)
INITIAL_SUPERUSER_EMAIL=admin@example.com
INITIAL_SUPERUSER_PASSWORD=ChangeMe123!

# Database
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=app

# Port configuration
BACKEND_PORT=8880
FRONTEND_PORT=8888

# Email / SMTP (used by utils/email/sender.py)
# Leave SMTP_HOST empty to disable sending in dev (emails will be logged to stdout).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=theodore.weerts@gmail.com
SMTP_PASSWORD=ebhe thgr bqcj nbxn 
SMTP_FROM_EMAIL=no-reply@totetrack.app
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# Public base URL used in links for verification and password reset emails
PUBLIC_BASE_URL=http://localhost:5173
